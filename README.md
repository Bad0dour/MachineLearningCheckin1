***************Machine Learning Check-in 1***************

**Main Python Code is found in 'Main.py'**

You can ignore everything else: (as those are just for me to save the files)
- virtual Environment ('.venv')
- Everything inside 'BoringStuff' Folder
- git ignore files ('.gitignore')

For Ease of use:
The Virtual Environment contains only the needed dependencies of the Main.py
There is also a list of those used needed dependencies inside of 'BoringStuff/requirements.txt'
